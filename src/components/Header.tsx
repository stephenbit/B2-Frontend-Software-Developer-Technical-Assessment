import React from 'react';
import './Header.css';

const Header: React.FC = () => {

    return (
        <header>
            <h1>User Directory</h1>
        </header>
    );
};

export default Header;
